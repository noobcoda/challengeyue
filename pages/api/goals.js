export default function handler(req,res){
    if (req.method === "POST") {
        //process POST request
        if (req.isNew){
            //make a new record

        } else {

        }
    } else {
        //Handle any other HTTP method
    }
}